import { DashboardShellComponent } from './dashboard-shell.component';

describe('DashboardShellComponent', () => {
  it('should be defined', () => {
    expect(DashboardShellComponent).toBeDefined();
  });
});
