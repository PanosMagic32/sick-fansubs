import { DashboardNavigationComponent } from './dashboard-navigation.component';

describe('DashboardNavigationComponent', () => {
  it('should be defined', () => {
    expect(DashboardNavigationComponent).toBeDefined();
  });
});
