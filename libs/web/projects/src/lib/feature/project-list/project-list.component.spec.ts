import { ProjectListComponent } from './project-list.component';

describe('ProjectListComponent', () => {
  it('should define the component class', () => {
    expect(ProjectListComponent).toBeDefined();
  });
});
